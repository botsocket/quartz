{
    "name": "@botsocket/quartz",
    "version": "1.0.0",
    "description": "Discord gateway connector",
    "main": "src/index.js",
    "types": "src/index.d.ts",
    "files": [
        "src"
    ],
    "repository": {
        "type": "git",
        "url": "git+https://github.com/botsocket/quartz.git"
    },
    "author": "BotSocket",
    "license": "MIT",
    "bugs": {
        "url": "https://github.com/botsocket/quartz/issues"
    },
    "homepage": "https://github.com/botsocket/quartz#readme",
    "scripts": {
        "lint": "eslint . --report-unused-disable-directives && echo \"No errors found\"",
        "lint:fix": "eslint . --fix && echo \"No errors found\"",
        "test": "jest --coverage && tsc && npm run lint && echo \"No errors found\"",
        "test:watch": "jest --watchAll"
    },
    "devDependencies": {
        "test": "jest --coverage && echo \"All tests passed\" && tsc && echo \"All type tests passed\" && eslint . --report-unused-disable-directives && echo \"No linting errors\""
    },
    "dependencies": {
        "@botsocket/bone": "^14.0.0",
        "@botsocket/jade": "^1.0.0",
        "ws": "^7.3.1"
    },
    "engines": {
        "node": ">=12.0.0"
    }
}
